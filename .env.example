cat > .env.example <<'EOF'
# Backend
API_HOST=127.0.0.1
API_PORT=8000

# Paths (local only; not checked in)
PHYSIONET_DATA_DIR=/absolute/path/to/protected_data

# Experiment tracking (optional)
MLFLOW_TRACKING_URI=http://127.0.0.1:5000
EOF
